#include "holberton.h"

/**
 * print_numbers - initialize the program
 * It works to print the only number digits
 * Not return
 */
void print_numbers(void)
{
	int a;

	for (a = 0; a < 10 ; a++)
		_putchar(a);
	_putchar('\n');
}
