#include "holberton.h"

/**
 * mul - prints a multiply of two numbers
 * @a : First integer
 * @b : Second integer
 * Return: Result of multiply two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
