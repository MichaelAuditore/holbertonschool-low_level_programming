#include "holberton.h"

/**
 * main - start program
 * this function display the alphabet in lowercase
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 'a' ; a <= 'z' ; a++)
		_putchar(a);
	_putchar('\n');
	return (0);
}
