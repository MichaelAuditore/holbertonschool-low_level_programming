#include "holberton.h"

/**
 * _strcpy - store a copy of a pointer into another pointer
 * @dest: Pointer one
 * @src: Pointer two
 * Return: Pointer
 */
char *_strcpy(char *dest, char *src)
{
	int i = 0;

	while (i[src] != '\0')
	{
		i[dest] = i[src];
		i++;
	}
	i[dest] = '\0';
	return (dest);
}
