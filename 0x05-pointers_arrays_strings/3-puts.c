#include "holberton.h"

/**
 * _puts - prints a string
 * @str: This is the string to evaluate
 * Return: Quantity of characters into a string
 */
void _puts(char *str)
{
	int i = 0;

	while (i[str] != '\0')
	{
		_putchar(i[str]);
		i++;
	}
	_putchar('\n');
}
