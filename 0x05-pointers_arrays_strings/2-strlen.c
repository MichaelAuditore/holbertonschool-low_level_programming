#include "holberton.h"

/**
 * _strlen - prints a string
 * @s: This is the string to evaluate
 * Return: Quantity of characters into a string
 */
int _strlen(char *s)
{
	int i = 0;

	while (i[s] != '\0')
		i++;
	return (i);
}
